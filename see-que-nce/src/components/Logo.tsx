import React from 'react';
import styles from './Logo.module.scss';

const Logo: React.FC = () => {
    return (
        <span className={styles.logoTitle}>
            see-quence
        </span>
    );
};

export default Logo;